<Window x:Class="UI.GuestList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="GuestList" Height="450" Width="800">
    <Grid>
        <ListView x:Name="listview" HorizontalAlignment="Left" Height="450" Margin="0,0,-8,-31" VerticalAlignment="Top" Width="800">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="כתובת מייל" Width="300" DisplayMemberBinding="{Binding email}"></GridViewColumn>
                    <GridViewColumn Header="שם משפחה"  Width="300" DisplayMemberBinding="{Binding lastName}"></GridViewColumn>
                    <GridViewColumn Header="שם פרטי"  Width="300" DisplayMemberBinding="{Binding firstName}"></GridViewColumn>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
